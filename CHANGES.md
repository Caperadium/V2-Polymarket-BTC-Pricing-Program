- Fix `ConvergenceWarning: Positive directional derivative for linesearch` in `btc_pricing_engine.py` `fit_garch_model()`: SLSQP optimizer returns code 8 intermittently during GARCH(1,1) fitting (scipy's fmin_slsqp linesearch stalls near flat gradient regions, common with heavy-tailed hourly BTC returns). Suppressed arch's raw warning via `show_warning=False`, added convergence-flag check after fit, and added fallback retry with relaxed tolerance (`ftol=1e-6, maxiter=500`) when convergence_flag is non-zero. A clean logger warning fires if both attempts fail; pipeline continues with best available parameters.
- Fix FileNotFoundError in Streamlit backtesting tab "Run Full Pipeline": resolve DATA_DIR and OUTPUT_ROOT relative to file location instead of CWD. `backrunner.py`, `contract_store.py`, and `orchestrator.py` used `Path("DATA")` / `Path("backtested_probabilities")` which fail when Streamlit changes the working directory. All three now use `Path(__file__).resolve().parent.parent.parent` to anchor to project root.
- Fix "Fetch Prices" re-downloading all contracts every click: staleness gate in `polymarket_fetcher.py` failed open when Gamma API returned null `closeTime` (common with event-nested markets), causing `should_fetch = True` for every known contract. Now uses precomputed `token_max_dates` dict (new `build_token_index()` in `contract_store.py` — one CSV load instead of 2+N) and falls back to stored-data recency check when `closeTime` is missing.
- Fix "Fetch Prices" re-fetching resolved contracts on every click: staleness gate used `max_stored < close_time` as proxy for "contract is open," but `max_stored` (midnight UTC from CLOB candles) is always < `close_time` (5 PM UTC expiry day) for resolved contracts, misclassifying them as open. Added `close_time > now` guard so only contracts with future close times are candidates for refresh. Also hardened `_parse_close_time()` to force UTC on naive timestamps from Gamma API.
- Fix spurious CRITICAL staleness warnings during time-travel backtesting: `load_and_prep_data()` in `btc_pricing_engine.py` checked intraday-data age against wall-clock time on every `calculate_probabilities()` call. During backtesting the truncated historical slice is always "stale." Added `disable_staleness_check=False` kwarg to `load_and_prep_data()` and `calculate_probabilities()`; backrunner passes `True` to suppress.
- Fix mass "no intraday data available" skips for timestamps before March 2026: `data_fetcher.py` `fetch_intraday()` defaulted to `days=90` (3 months). Changed to `days=730` (2 years) to cover full backtest window. Added earliest-timestamp gate: if existing file's first row is past the requested `start_cutoff`, force a full re-fetch instead of taking the incremental path which only appends forward. Run `python core/data/data_fetcher.py` to backfill historical intraday data.
- Fix backrunner skipping all pre-March-2026 timestamps when intraday data hasn't been backfilled: added daily-close fallback in `backrunner.py`. When `intraday_slice` is empty, the pricing engine now uses the last daily close price as S0. Intraday data is only used for current price; GARCH runs on hourly data. Also added one-time INFO log when fallback activates. Docstring updated.
- Fix `RuntimeWarning: invalid value encountered in divide` in `btc_pricing_engine.py`: removed dead code in `_compute_figarch_weights()` that computed unused `log_weights` variable via an incorrect gamma ratio. The actual FIGARCH lambda weights are correctly computed via recurrence. Removed 5 lines of dead code and consolidated the theoretical formula comment.
- Fix dashboard green success box when pipeline produces 0 trades: `app/pages/backtesting.py` now shows `st.warning()` when trades_df is empty, with instructions to check BTC data coverage. Success message also improved to only show new_records count when fetch was run (not always 0 for fetchless "Run Full Pipeline" path).
- Parallelize backrunner MC pricing loop: `backrunner.py` refactored with `ProcessPoolExecutor`, per-worker BTC data loading via `_init_worker()`, BLAS thread suppression (OMP/MKL/OPENBLAS/NUMEXPR=1), deterministic per-timestamp seeds via `hashlib.md5`, and S0 daily-close fallback pre-computed in main process. New `--workers N` and `--serial` CLI flags. Default parallel workers = `min(cpu_count - 4, 12)`. Estimated speedup: ~8.4 hr serial → ~42 min parallel (12 workers on 20-core machine). Orchestrator/dashboard unchanged — continues serial by default.
- Fix `AttributeError: 'ContractPriceStore' object has no attribute 'df'` in `backrunner.py` `load_market_df()`: `ContractPriceStore.load()` now returns the DataFrame instead of storing on `self.df` (API change from contract_store refactor). Captured return value directly.
- Fix FIGARCH negative variance → NaN + RuntimeWarning in `btc_pricing_engine.py`: Three-layer fix. (B) `_compute_figarch_weights()` now accepts `beta` parameter and produces correct FIGARCH(1,d,1) ψ_k filter weights via (1-βL)⁻¹ convolution of the raw (1-L)^d binomial weights — previously applied raw zero-sum λ_k weights directly, which could drive variance negative. (A) Safety floor `np.maximum(variances, 1e-10)` after both FIGARCH and GARCH variance updates (line 646). (C) Initial variance guard `np.maximum(last_variance, 1e-10)` in `simulate_paths()`. Updated test 7 signature and DOCS reference. All 9 self-tests pass.
- Fix `RuntimeWarning: overflow encountered in exp` at line 688 in `btc_pricing_engine.py` (surfaced after FIGARCH NaN fix allowed simulation to complete): Extreme Student-t tail draws (10σ+ with low nu) produce cumulative `log_prices > ~709` via variance feedback cascade. Two-level clip: `np.clip(total_log_return, -2.0, 2.0)` per step (±640% hourly, BTC worst ~40%) and `np.clip(log_prices, -50.0, 50.0)` before `np.exp()`. Thresholds are bias-free — zero probability mass beyond them. Reviewer note: epsilon_squared at L628 still gets raw z_t (before clip), so variance cascade persists but causes only noise, not crashes.
- Fix backrunner running serial (apparent hang) from dashboard "Run Full Pipeline": the parallel `ProcessPoolExecutor` path existed but only the CLI passed `workers`; `orchestrator.run_pricing_backrun()` called `backrunner.run()` with no `workers` arg → always serial. Threaded `workers` through `orchestrator.run_pricing_backrun()` and `run_full()` (new `default_worker_count()` helper = `min(cpu_count-4, 12)`, mirrors CLI), wired a "Worker Processes" input into the Live Fetch sidebar of `app/pages/backtesting.py`, and pass it to `run_full(workers=...)`. Backrunner clamps `workers` to work-item count. Added a UI warning when Timestamp Limit = 0 (backruns all 4801 timestamps). Also optimized `_preprocess_work_items()`: replaced O(n_ts × n_rows) per-timestamp boolean masking of intraday/daily data with `np.searchsorted` on precomputed int64 index arrays (O(log n) lookups). Verified parallel path on Windows (3 workers → 6/6 batch CSVs).
- Fix Streamlit warning flood (and latent `python -c` fork-bomb) from parallel backrun: enabling parallel from the dashboard made `orchestrator.run_pricing_backrun()` create an in-process `ProcessPoolExecutor`. On Windows (spawn) each worker re-imports the parent `__main__` — under Streamlit that's the page script (re-runs `st.set_page_config`/widgets outside the runtime → "Session state does not function..." warnings ×N workers); under `python -c "...run_full()"` it would recursively spawn. Orchestrator now routes any `workers > 1` backrun through a clean subprocess of the backrunner CLI (which has a proper `__main__` guard, so its spawn children import `core.backtesting.backrunner`, not the page). Child stderr `Progress: X/Y` lines are parsed to drive the UI progress callback. Custom (non-default) backrun dirs fall back to serial with a warning (CLI has no dir flag). In-process pooling is now reserved for the CLI entrypoint only. Verified: 4 workers dispatch real MC work, callback fires, no warnings, no fork-bomb.
- Fix ~1-day look-ahead bias in backrunner daily-close S0 fallback (pre-2026-03-01 timestamps, before intraday data starts): `btc_daily.csv` rows are indexed at day-start (D 00:00) but `close` holds the END-of-day-D price (≈ D+1 00:00). `_preprocess_work_items()` selected row D for a D-00:00 contract, so S0 was ~24h in the future (model "saw" the day's close at the day's open, inflating backtest accuracy for those dates). Now slices with `searchsorted(..., normalize(ts), side="left")` to use the prior day's close (the price actually known at the contract midnight) — verified: S0 for 2026-03-02 corrected from 68830 (lookahead) to 65776, matching the true midnight intraday spot of 65770. Post-2026-03-01 timestamps use exact 1m intraday and were unaffected. Deleted 441 unfitted + 393 fitted pre-March batches that carried the stale lookahead prices so they regenerate correctly on the next backrun.
- Statistical/mathematical validity fixes to the BTC price modeller (`core/pricing/btc_pricing_engine.py`), each behind a flag or scoped to opt-in features so the production default path is unchanged (verified: P(above) grid byte-identical for the default config across 1d/1w/30d × 5 strikes). Reviewed via plan-reviewer before implementation. (1) Jump-drift compensator: added `martingale_anchor` flag (default False = legacy log-mean `λ·E[J]`) that switches to the true exponential-cumulant risk-neutral compensator `λ·(E[e^J]−1)` for Kou jumps, with `eta_up>1` guard + warn-fallback; threaded through `calculate_probabilities` and into `_meta`. (2) SVCJ block rewrite (Eraker 2004): return and variance jumps now share the SAME Poisson count `k` (was an independent `k_v` draw that broke contemporaneous correlation); variance-jump magnitude is `Gamma(k, μ_v)`; the `N(0,σ_s)` stochastic residual is now masked to jumping paths only (previously leaked into every path every hour, inflating terminal vol ~27% over 30d — Test 6 SVCJ std 0.296→0.123). Removed dead `lam_v_hourly`. (3) Skewed-t sampler: replaced the ad-hoc piecewise scaling of a raw `standard_t` (non-Hansen distribution with a nonzero mean → unintended drift) with Hansen (1994)'s proper inverse-CDF construction (mean 0, variance 1 analytic; `skewed_t_scale_factor` now returns 1.0). Eliminated the drift that had pushed P(above ATM, 30d) to 0.988; now ~0.535. (4) FIGARCH positivity: `fit_garch_model` now applies a Bollerslev-Mikkelsen non-negativity check on the ψ_k weights and warns+falls back to GARCH (clearing `figarch_weights`) instead of silently flooring negatives at 1e-10; note the Siu d=0.578 default fails this and disables FIGARCH by default. (5) Regime mixture: switched from fixed `n_sims//3` per regime (effective sample dropped to ~n_sims/3 when one regime dominated) to path allocation proportional to HMM posterior weight summing to n_sims, with distinct per-regime sub-seeds for independence. Tightened Test 9 to assert mean~0 (drift regression guard) and variance∈[0.96,1.04]; all 9 self-tests + 51 pytest tests pass.
