# Changes

<!-- Append one entry per logical task. Cleared after each push. -->

- Fix mm-monitor docs: the dashboard page needs `plotly` as well as `streamlit`; the pip line in `deploy/README.md`'s mm-monitor section and `DOCS/guides/market-maker-deployment.md` now installs both.

- Fix mm_monitor state-db resolution for runs launched with `--state-db`: the page hardcoded `<out_dir>/paper_state.db` (the per-run default), so a persistent db like the VPS config's `market_maker/mm_paper_state.db` was never found ("paper_state.db not created yet" on every panel). `app/pages/mm_monitor.py` gains `resolve_state_db(out_dir, run_meta)` -- prefers the new top-level `state_db` key in run_meta, then the config dict's `state_db`, then a `--state-db` in the recorded argv, then the per-run default; relative paths anchor at the repo root. `market_maker/paper_runner.py` now resolves `state_db_path` before writing run_meta.json and records it as `run_meta["state_db"]`. Tests: `tests/test_mm_paper_runner_control.py` asserts the recorded path for both the default and `--state-db` cases (396 MM tests pass).

- Market-maker fix batch: wire plan-mandated machinery found dead by the 2026-07-08 architecture review (temp/architecture_review.md; spec temp/mm_fix_plan.md, plan-reviewer GO after 2 rounds; final diff review CLEAN; 608 tests pass, was 564). Three waves:
  - Wave 0 (behavior-safe): paper_runner resume now consumes `loop.restart()`'s ReconciliationResult (discrepancies logged, MANUAL risk-journal rows, `resume_discrepancies` in heartbeat, `manual_override` held until first clean tick; redundant `mark_all_live_orders_unknown` call dropped); `MMStateStore.prune_quotes` + `quotes_retention_s` config (14d) called at report cadence (plan Section 5 "pruned by age" was never implemented); `InventoryManager.mark(now)` called every tick so `age_weighted_holding`/R3 accrue between fills; dead `PaperFillSimulator.mark_fills` + `_ADVERSE_HORIZONS` deleted (superseded by the mid_log markout report; fills-table mid_p* columns kept legacy-NULL); ticks.csv gains `snapshot_failed` column; `kelly_mult` documented as journaled-only by decision (not applied to sizing).
  - Wave 1 (risk-tightening): DEPTH sizing cap wired (`last_liquidity` -> `_compose_quote_sets` -> `size_ladder`; `append_liquidity_window` persisted + pruned); fair-value staleness handled (`RiskTrigger.FAIR_VALUE_STALE`, widen at new `fv_max_age_s`=300s, PULL at 2x; `_x_hist` append skipped on non-recompute ticks so sigma_b no longer decays to floor on a frozen anchor); bankroll auto-unfreeze after `bankroll_unfreeze_clean_ticks`=20 consecutive clean BEUOY recomputes (`bankroll_frozen` in heartbeat + mm_alert_check alert); phantom config promoted to real MMConfig fields (`bankroll_floor`=0.02, `risk_latch_seconds`=60.0).
  - Wave 2 (vertical hedging wired, plan 2.6/2.7(b)/6.2): new `ladder_hedger.hedge_offsets_by_market(recs)` builds the market_id-keyed HedgeState (the bucket-keyed second return is audit-only); harness hedge stage after fills routing calls `vertical_hedges`, feeds `set_hedge_state`/`set_phi`, and stores pending recs; next tick the recs skew neighbor quote sizes post-no-arb-repair under an exhaustive side-scale max_price rule (BUY_YES iff bid<=max_price; BUY_NO iff 1-ask<=max_price), never resurrecting a directive-suppressed side; `upsert_ladder_state` persisted at report cadence; `enable_beta_hedge` call site added (default off, asserted inert). `last_quote_sets` now journals the post-repair/post-skew ladder.
  Files: market_maker/{harness,paper_runner,state_store,config,contracts,risk_controller,ladder_hedger,paper_fill_sim}.py, scripts/mm_alert_check.py, tests (+44 tests). Note: VPS acceptance run must restart on a fresh state-db (behavior changed).
